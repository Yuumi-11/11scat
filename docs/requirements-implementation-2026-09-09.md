# 需求表本轮实施记录

日期：2026-09-09。用户随后明确授权将本轮工作流程与任务板修改推送上线。本次从最新线上版本单独接入该工作单元，保留线上印章及语音更新；教室整体外观继续保存在本地外观分支，不随此次发布。

## 已实现内容

| 优先级 | 需求 | 当前行为 |
| --- | --- | --- |
| 0 | 认领者保留删除权限 | 工作流程详情提供“删除我的任务”，确认后只删除认领者收集箱中的关联任务。保留审批进度与附件，刷新后标示任务异常；手动恢复只补回认领者任务。删除响应丢失可以重试，旧请求不会删除后来恢复的任务。 |
| 0 | 按发起者实际任务完成同步 | 公共任务认领只创建认领者任务。发起者没有关联任务时，提交、审批、直接完成及详情同步均可继续；不补建发起者任务。现有发起者关联任务仍保留版本校验和正常同步，读取失败不会当作任务不存在。 |
| 0 | 待审批置顶 | 待审批流程优先于所有其他未完成状态；同一组内保留未读优先及原时间排序。归档列表仍独立显示。 |
| 0 | 便签拖动实时跟随 | 拖动预览改为完整便签，保持按下位置相对纸面的偏移和原卡片宽度，在指针移动时更新。预览移到不会被任务容器裁切的位置；取消拖动清除预览，有效目标栏放下后认领。 |
| 1 | 便签宽度和上方空白 | 公共任务网格的最小宽度恢复为 205 像素，便签正文顶部内边距缩为 8 像素，保留上沿拖动区域。 |
| 1 | 工作流程详情弹窗 | 取消详细设置二次入口，左栏约占 62%，右栏直接展示可编辑设置。催办与直接完成同排，状态标记归入左栏；窄屏改为上下排列。未保存设置保留原版本，发生并发更新时要求重新载入，避免覆盖新内容。 |
| 2 | 历史记录显示 | “安排认领”改为“认领”。整条“完成同步”事件不显示，也不形成无法在界面消除的未读提示；后台完成动作和持久化事件保留，提交完成及审批通过记录继续显示。 |

## 本轮验证

生产构建通过，124 项自动检查全部通过，无失败或跳过。检查包含认领者删除权限、删除后刷新、丢失响应重试、恢复后的旧请求保护，以及发起者任务在提交前或提交后缺失的情况；原有重复任务与审批版本检查也通过。音频检查使用放在 `codex-generated/verification-tools` 的临时验证工具，未修改项目依赖。

修改范围内的静态检查没有错误，两条既有测试变量警告保留。构建仍有原有的文件追踪范围警告，本轮没有调整这项独立配置。

浏览器在独立的示例数据页面复用正式协作组件和任务存储逻辑，不连接真实滴答账户。已确认待审批排序、详情表单直接显示、左右栏宽度约 604 与 371 像素，以及修改标题后保存成功并回到需要重新提交的状态；跨栏拖动后认领者任务数正确增加，发布者收集箱保持为空。未获得松开鼠标之前的连续画面证据，因此实时跟随的最终观感仍保留为本地验收项，不以自动检查替代真机触控验收。

## 仍需等待的内容

用户明确指出本次长尾夹图不符合要求，并要求等整体外观完全确定、正式上线之后再制作。生成图已从正式素材目录移出，页面没有长尾夹图片引用；不再尝试替代图。

持续摇铃的手机和手表验证、网易云状态读取仍按已有交接由远端开发者处理。语音转文字需要服务器维护者配置授权并验证真实录音；教室外观及真实双人媒体、触控验收仍待完成。上述事项继续保留在剩余需求表，本轮未重复发送交接邮件。发布沿用 GitHub Actions 与既有服务器部署流程。
