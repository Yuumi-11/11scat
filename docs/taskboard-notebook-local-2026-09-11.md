# 任务板视觉更新：本地版本

更新时间：2026-09-11。当前地址：`http://localhost:3008/classroom-preview/taskboard`。预览无需登录，使用演示任务；操作只改变当前页面数据，刷新恢复。所有外观修改仅在本地，不推送或部署。

## 公告板与图钉

上方采用软木公告板，参考用户提供图片的形式和材质，保留原来的便签与图钉配色。`public/classroom/taskboard-cork.svg` 为确定性生成的原创可编辑软木碎屑纹理，没有使用参考图片的像素。便签图钉复原到历史提交 8578150 中的几何：10 像素直径，纸面上沿下方 7 像素，水平居中，使用主题色和原高光、阴影。拖动区域仍覆盖图钉周围 48×32 像素。长尾夹不使用。

## 摊开的白色笔记本

两名成员各占一页，去除活页环及外皮，纸页延伸到弹窗内容边界，中缝仅保留轻微弧度与明暗，模拟平摊的线装本。纸张和任务卡片均为纯白。保留每页的有日期、无日期两栏以及中间分栏线。

姓名栏保留 `title:`，横线最长 180 像素，不铺满整行，去掉“我”。中英文采用今日任务同一组手写字体：School Caveat、Classroom Yan、Long Cang。

## 卡片布局与操作

下方卡片没有可见图钉和省略号按钮。任务名称单击打开已有任务详情，不在列表中编辑；名称与日期、操作按钮排列在同一行，名称占据剩余宽度，超长直接截断。标注中的左边距 -2 像素已落实；右侧约 49 像素分配给日期组件而非空白边距，无日期时名称自动取得这部分宽度。认领按钮位于原省略号位置，采用白底、细边框的平面样式。

优先级改为卡片左缘红、橙、蓝色竖条，无优先级没有标记。上方公共便签仍使用此前确定的小旗。日期优先显示今天到大后天，其后为这周或下周，再其后只显示月日；具体时间继续保留。以上海时区和周一为周起点，日期来源及排序保持截止优先、开始兜底。

下方卡片非交互区域可以拖动，按钮、输入框和编辑控件不触发卡片拖动。与图钉共用启动阈值、跟随、边缘滚动和放下逻辑；拖动启动后在窗口范围跟踪指针，避免指针越过原卡片时漏掉移动和松开事件。手势结束、取消、关闭或卸载时移除监听。保留无可见图钉的键盘拖动入口。

## 验证记录

已在本地浏览器核对公告板、白色纸页与任务卡片、短姓名横线、同排日期及名称点击进入详情。浏览器拖动发现跨卡片后事件被忽略，已改用窗口级监听，新增检查覆盖跨元素事件、另一根手指、取消和监听清理。最终浏览器拖动复核被自动审批拒绝，未提供具体原因，故修正后的真机拖动仍待验收。
