# 任务设置截图批注落实

详细设置底部的删除入口与标签输入框左侧对齐，使用红色垃圾桶图标并保留无障碍名称，保存修改按钮保持在右侧。原有删除权限和两次点击确认保留，确认说明不会改变原任务的删除范围。

任务附件点击统一打开带下载入口的预览弹窗，移除上传成功后的常驻说明文字。粘贴后的文件先暂存，保存修改时加入云盘，删除附件并保存后清理对应文件；具体规则见[任务附件说明](task-description-attachments-2026-09-10.md)。流程中新旧附件变动均以文件名和扩展名显示。

自动检查覆盖暂存读取权限、保存前云盘不存在、保存后其他成员可读取、下载响应与文本分段读取，以及保存移除后的文件清理。存储层验证了旧附件、共享引用和失败重试；组件检查覆盖删除按钮无可见文字、附件不打开新页面和历史链接的文件名显示。所有检查使用隔离模拟账户，未操作真实滴答任务。

2026-09-11 最终验证：生产构建通过，完整测试共 139 项，全部通过，无失败或跳过；修改文件的静态检查无错误，仅附件图片原生预览保留一条 Next.js 图片优化建议。交互检查验证了点击后的文字预览、下载入口、Escape 只关闭附件弹窗、保存后才发送附件移除修改，以及粘贴调用暂存接口。HTTP 检查同时确认 PDF 的内嵌响应不使用会阻止浏览器 PDF 查看器的沙盒头，其他附件保留原有响应限制。

内置浏览器连接失败，因此尚未完成真实浏览器截图与视觉验收。以上交互结果来自组件环境和真实 HTTP 接口测试，不作为浏览器实际呈现的验收记录。
