# 印章与语音转文字更新

日期：2026-09-09。

认领流程的原任务方看到只含认领者名字的红色印章；英文数字采用 Protest Revolution，中文采用龙藏体。字体本地托管并附 OFL 1.1 授权。印章倾斜六度，下方和右方部分边框被便签边缘裁去，文本留出内边距并支持长名字换行。印章没有按钮行为。

网站取消每日180分钟限制，旧缓存迁移后继续保留，旧每日用量及全站次日锁定不再生效。识别请求最多三条并行，最多六条不同语音在处理或排队；音频转换及短记录事务各自依次执行。平台402/429按响应提示和Retry-After处理，未提供时该条请求冷却一分钟，不暂停全站到次日。不自动重试，不启用付费回退，仍要求维护者确认免费计划。

单条305秒、4 MiB、远端60秒超时等运行保护保留。真实语音仍需服务器维护者配置Cloudflare授权后验收，本轮未调用真实账号或上传用户录音。

需求表中已完成移除便签上的催办按钮、任务说明前置两项，详情内催办保留。自由拖动定位、完整双栏设置，以及等待教室外观完成的长尾夹素材仍保留待办；持续摇铃和网易云同步沿用既有交接。

生产构建和修改文件ESLint通过。全量测试最初113项通过，1项因本机未指定FFmpeg路径失败；补充已安装工具路径后该项单独通过。随后新增并行失败恢复测试，与全部7项语音服务测试一起通过。验证覆盖三请求并行、准备串行、乱序结果完整、缓存重启复用、旧每日限制迁移、超过原180分钟仍可请求、限流重试及并行失败不影响其他结果。
