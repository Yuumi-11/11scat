# 教室与任务板正式发布

日期：2026-09-11。用户已确认任务板外观完成，并明确允许教室更新上线；原有仅限本地的要求至此被替代。随后确认设置只保留“通用”中的座位，外观设置与消息铃声提醒入口均删除，其他设置项等后续需要再增加。

## 发布范围

- 正式首页使用已确认的教室外观，黑板沿用“稍深”底色；平板、笔记本及各桌面物件保持最终批注版本。任务板数字角标继续相对实际素材定位，小半覆盖在任务板右上角。
- 点击任务板打开软木公告板与白色笔记本的新版界面，保留线上已发布的附件保存与预览、手动审批、发起者删除及印章字体更新。附件文件名入口继续显示在卡片中。
- 设置沿用白底左右分栏，通用栏仅含平板电脑和笔记本电脑两个座位选项。选择后保存才改变双方座位；关闭、Escape 或点击弹窗外放弃本次草稿。保存失败保留草稿，重复保存相同目的座位不会反向交换。
- 日历入口点击无操作，等待后续双人日历功能。云盘、摇铃和任务详细设置继续使用现有功能界面，已确认的摇铃实体入口外观保持不变。
- 投屏直接打开浏览器共享选择，由浏览器提供音频选择。开发预览继续使用示例任务，正式首页沿用身份验证与真实接口。

## 集成与验证

在独立发布目录中以线上 `5be2787` 为基准合入本地外观 `99ebb4a`，保留两边已完成工作。原开发预览和其他工作目录不受影响。

最终生产构建通过，179 项自动检查全部通过，没有失败或跳过。另复用已有组件检查，验证设置草稿、关闭放弃、重复保存保护、失败重试，以及任务板入口的打开、关闭和再次打开。合并涉及的源文件静态检查无错误，首页仍有 5 条既有警告。首次测试因验证进程缺少 FFmpeg 路径以及旧测试仍要求已取消的通知开关而失败，补齐临时验证工具路径并按最新需求更新旧断言后，完整检查通过。

生产发布沿用现有 GitHub Actions、GHCR 与服务器流程，发布目标为 https://study.11scat.xyz/ 。发布后的完成判据为部署任务确认运行本次精确版本，并独立核对登录页与新增字体、材质资源的状态、内容类型及文件摘要；运行日志保存在本地 `codex-generated`。本次不将自动检查表述为真实双设备投屏、通话或手机手表验收，相关后续事项保留在剩余需求执行表。
